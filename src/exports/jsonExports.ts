/**
 * Export File:
 * This file serves as a centralized export point for all jsons
 * used throughout the application. By organizing and exporting classes from
 * different files into categories, it facilitates easier imports elsewhere
 * in the codebase with just one import statement, promoting modularity and
 * maintainability.
 */

// Base URL
export { default as baseUrlJson } from '../resources/testData/baseUrl.json'

// Login Credentials
export { default as loginCredentialsJson } from '../resources/testData/loginCredentials.json'
